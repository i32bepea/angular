import { ConvertToSpacesPipe } from './convert-to-spaces.pipe';

describe('ConverToSpacesPipe', () => {
  it('create an instance', () => {
    const pipe = new ConvertToSpacesPipe();
    expect(pipe).toBeTruthy();
  });
});
